<Page x:Class="KanaFrame.PageSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KanaFrame"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="PageSymbolSettings">
    <Grid>
        <Border Style="{StaticResource StdBorder}" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="btn_cancel" Content="Cancel" Click="btnCancelClick"/>
                    <Button x:Name="btn_ok" Content="OK" Height="30" Click="btnOkClick"/>
                </StackPanel>
            </StackPanel>
        </Border>


        <Border Style="{StaticResource StdBorder}" VerticalAlignment="Top">
            <StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
